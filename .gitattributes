# Treat source files as text
*.py text
*.html text
*.md text

# Handle line endings automatically
* text=auto

# Large model files (prepared for Git LFS if used)
*.bin filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text
